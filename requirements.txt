# Barangay Management System (BMS) Requirements
#
# Python Standard Library:
# - Python 3.8+ (tested on Python 3.12)
# - No external pip packages required for basic runtime or SQLite API operations.
# - Optional: python-dotenv (pure Python parser is built-in to config.py)
#
# Optional enhancement libraries:
# - Pillow >= 10.0.0 (enables server-side image watermarking on uploaded photos; falls back to CSS watermark if absent)
Pillow>=10.0.0
